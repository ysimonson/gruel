[workspace]

[package]
name = "rust-third-party"
version = "0.0.0"
publish = false
edition = "2024"

# Fake binary to satisfy Cargo
[[bin]]
name = "fake"
path = "/dev/null"

[dependencies]
annotate-snippets = "0.11"
libtest-mimic = "0.8"
serde = { version = "1", features = ["derive"] }
tempfile = "3"
toml = "0.8"
